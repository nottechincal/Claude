# Flask web framework (required)
flask>=2.3.0

# CORS support for Flask (required)
flask-cors>=4.0.0

# Environment variable management (required)
python-dotenv>=1.0.0

# Twilio for SMS notifications (optional)
twilio>=8.10.0
